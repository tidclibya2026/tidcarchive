# حزمة التنصيب المحلي لنظام TIDC

هذه الحزمة مخصصة لخادم يعمل بنظام **Windows 10/11 Pro أو Windows Server** في جهاز الشؤون الإدارية، في المسار المقترح `C:\Archiving`. تشغّل الحزمة التطبيق وقاعدة MySQL وتخزين MinIO وعامل OCR محلياً داخل حاويات معزولة. لا تتضمن الحزمة بيانات المعاملات أو صور المستندات الموجودة في بيئة التجارب؛ تُنقل تلك البيانات فقط من نسخة احتياطية معتمدة.

## المتطلبات

يتطلب الخادم اتصالاً داخلياً ثابتاً وشغّالاً أثناء الاستخدام، وذاكرة لا تقل عن **12 GB** ومساحة حرة لا تقل عن **60 GB**. يشمل ذلك هامشاً مناسباً لخدمة فحص الملفات؛ إذ تتطلب قاعدة تواقيع ClamAV ذاكرة ملحوظة أثناء التحميل والتحديث.[1] يجب تثبيت **Docker Desktop** وتشغيله مع تفعيل WSL 2 أو استخدام Windows Server مع محرك حاويات متوافق. يلزم فتح المنفذ الداخلي الذي يحدده المسؤول، ويكون `8080` افتراضياً.

## التنصيب الأول

انسخ كامل مشروع النظام إلى `C:\Archiving`، ثم شغّل الملف `ops\local-windows\Install-TIDC.cmd` بالنقر المزدوج. يستخدم الملف PowerShell مؤقتاً لتجاوز سياسة التنفيذ للحلقة الحالية فقط، ثم يُنشئ ملف الأسرار المحلي ويبدأ الخدمات. يمكن للمسؤول بدلاً من ذلك تشغيل الأمر التالي من PowerShell بصفته مسؤولاً:

```powershell
Set-ExecutionPolicy -Scope Process Bypass
C:\Archiving\ops\local-windows\Install-TIDC.ps1
```

تطلب أداة التنصيب بريد المدير وكلمة مرور جديدة لا تقل عن عشرة أحرف. تُنشأ مفاتيح قاعدة البيانات والتخزين وOCR عشوائياً، وتُحفظ في `ops\local-windows\.env`. لا يُرفع هذا الملف إلى Git ولا يرسل بالبريد.

بعد اكتمال التنصيب، افتح عنوان HTTPS الذي يظهر في نافذة التنصيب، ويكون مثل `https://tidc.ly:8443` في الإعداد الداخلي الافتراضي. للوصول من جهاز آخر بالشبكة الداخلية، استخدم اسم DNS المعتمد للخادم مع المنفذ نفسه بعد تثبيت شهادة الجذر الداخلية على أجهزة المركز وإعداد جدار حماية Windows. يبقى `tidc.com.ly` خياراً منفصلاً للنمط العام المعتمد فقط؛ راجع `docs/TIDC_DOMAIN_DEPLOYMENT.md` قبل اختياره.

> عند فتح الرابط لأول مرة في متصفح لا يملك جلسة صالحة، تظهر **شاشة تسجيل الدخول**. أما الجلسة المسجلة سابقاً فتعيد المستخدم إلى مساحة عمله مباشرة.

## التشغيل والصيانة

| الملف | الوظيفة |
|---|---|
| `Install-TIDC.cmd` | نقطة التنصيب المباشر بالنقر المزدوج على ويندوز. |
| `Start-TIDC.cmd` | بدء النظام بعد إعادة تشغيل جهاز الخادم. |
| `Stop-TIDC.cmd` | إيقاف الخدمات دون حذف البيانات. |
| `Show-Logs.cmd` | عرض آخر سجلات التطبيق وMySQL وMinIO وOCR للتشخيص. |
| `Test-TIDC-Installation.ps1` | تحقق من صياغة الحاويات وحالة الخدمات وعنوان HTTPS والرؤوس الأمنية. |
| `docker-compose.yml` | تعريف الخدمات المحلية؛ لا يعدّل إلا بواسطة مسؤول النظام. |

لا تستخدم `docker compose down -v` لأن الخيار `-v` يحذف أقراص قاعدة البيانات والمرفقات. النسخ الاحتياطي المشفر والاستعادة المعزولة يتطلبان تهيئة جهة تخزين خارجية ومفاتيح تشفير معتمدة، ثم تطبيق إجراءات `docs/LOCAL_BACKUP_AUTOMATION.md` في بيئة تشغيل مخصصة.

تعمل خدمة ClamAV داخل شبكة الحاويات فقط ولا يفتح منفذها إلى شبكة المركز. يرفض التطبيق أي رفع عند اكتشاف ملف ضار أو عند تعذر فحصه؛ وتبقى الملفات غير مخزنة إلى أن تنجح عملية الفحص. يجب إبقاء خدمة الحاوية وقواعد تواقيعها محدثة وفق نافذة الصيانة المعتمدة.[1]

تشفّر حزمة ويندوز كل مرفق قبل تخزينه في MinIO باستخدام **AES-256-GCM** ومفتاح مستقل بطول 256 بت ينشأ عند التنصيب ويحفظ في ملف الأسرار المحلي. لا يكفي نسخ أقراص MinIO لقراءة المستندات دون هذا المفتاح. يجب نسخ المفتاح بصورة مشفرة ضمن إجراء النسخ الاحتياطي واسترجاعه قبل أي استعادة. يوفر MinIO أيضاً طبقات تشفير خادمية مدارة بالمفاتيح؛ يتطلب تفعيلها خادماً مستقلاً لإدارة المفاتيح ولا ينبغي تمكينها بمفتاح مضمّن داخل ملفات المشروع.[2]

تتصل أجهزة المستخدمين ببوابة Caddy عبر HTTPS فقط. وتبقى MySQL وMinIO وOCR وClamAV داخل شبكة الحاويات، ولا تُنشر منافذها إلى شبكة المركز. تمنع هذه البنية الاطلاع المباشر على قواعد البيانات أو الملفات من الأجهزة العميلة.

## نقل البيانات المعتمدة

قبل تشغيل النظام على الشبكة الداخلية، تنقل إدارة التقنية فقط نسخة MySQL ونسخة MinIO معتمدتين ومشفرتين، ثم توثق اختبار الاستعادة. لا تنقل ملف `.env` من بيئة أخرى ولا تستخدم كلمات مرور تجريبية.

## المراجع

[1] [ClamAV in Docker — متطلبات الذاكرة، تحديث التواقيع، والمنفذ الداخلي لخدمة الفحص](https://docs.clamav.net/manual/Installing/Docker.html)

[2] [MinIO Server-Side Encryption — نموذج المفاتيح ومتطلبات KMS لتشفير SSE-S3](https://github.com/minio/minio/blob/master/docs/security/README.md)
